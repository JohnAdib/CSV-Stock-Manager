{
  "name": "csv-stock-manager",
  "version": "1.0.0",
  "description": "A simple CSV stock manager",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "prettier": "prettier . --write",
    "prettier:check": "prettier . --check",
    "test": "turbo test",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "turbo": "^2.1.3"
  },
  "workspaces": [
    "ui/",
    "backend/"
  ],
  "packageManager": "npm@10.9.0",
  "engines": {
    "node": ">=20"
  }
}
